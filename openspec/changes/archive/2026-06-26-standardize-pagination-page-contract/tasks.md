## 1. 通用分页能力

- [x] 1.1 在公共层新增统一分页请求 DTO，固定定义 `pageNo` 和 `pageSize` 及其校验规则
- [x] 1.2 在公共层新增统一分页返回类型，固定定义 `total`、`list` 和 `hasNext`
- [x] 1.3 为仓储层设计可复用的分页查询契约，明确分页参数与分页结果的传递方式

## 2. 招聘职位列表改造

- [x] 2.1 将有效的职位列表控制器入口改为 `xxxPage` 语义命名，并接收统一分页参数
- [x] 2.2 改造职位应用服务，使其返回统一分页结果而不是原始数组
- [x] 2.3 改造职位仓储接口与 MikroORM 实现，支持总数统计和按页查询

## 3. 契约对齐与验证

- [x] 3.1 更新职位分页接口相关测试，覆盖合法分页参数与统一返回结构
- [x] 3.2 增加分页空结果场景测试，验证 `total`、`list` 和 `hasNext` 的固定语义
- [x] 3.3 检查并更新接口文档或调用方约定，明确这是一次破坏性分页契约调整
